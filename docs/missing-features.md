# 可能缺失的功能 · Missing Features

> 按优先级分组。依据代码证据（存在/缺失），并对照「Beta 风险清单」逐项检查。标注：Beta 必须（阻塞）/ 建议 Beta 前 / 可延后。
> 最近更新：2026-08-13。本轮：**多设备玩家数据同步（M01）已从当前范围移除（可忽略，见下）**；补充卡图 ZIP 导入能力；其余既有项 M09/M12 已解决。

## 范围说明

- **多设备同步为「设计定稿，待实施」**：`docs/player-data-sync.md` 已按 v2 重构（Bundle 基线 + Git 连续 + Supabase 远期，范围收窄、稳定键引用）。设计已重新纳入，**但实施不在本次 Beta 范围内**；换机/重装迁移仍由「一键完整数据包 + 恢复前校验」覆盖。
- **卡图 ZIP 导入已实现**：设置 → 本地卡图缓存 →「导入卡图 ZIP」，选择 .zip（应用自身缓存导出格式）自动匹配 `card_prints` 并导入缺失卡图（匹配优先级：应用缓存精确名 → printCacheName → card_no_extend；已存在跳过）。

## 中优先级（Beta 期间可补，建议尽早）

### M09 借还 / 联系人 CSV 导出（✅ 已解决）

- 证据：新增 `src/lib/collection/loan-csv.ts`（`buildLoansCsv` / `buildContactsCsv`，复用 `buildCsv`）；`/collection/loans` 页 topbar「导出」动作 + 弹窗两个选项（借还记录 CSV / 联系人 CSV），借还导出当前筛选结果（含联系人名/状态/工艺本地化文案），复用 `saveTextFile`；i18n `loans.export*`。
- 是否 Beta 必须：已解决。可否延后：已解决。

## 低优先级（可延后）

### M11 应用自动更新（未实现）

- 证据：无 `tauri-plugin-updater`（Cargo/package/capabilities 均无）。
- 是否 Beta 必须：否，Beta 人工分发新包即可。可否延后：延后。

### M12 规则列表 / 系列收藏页空状态（✅ 已解决）

- 证据：`/rules` 页 `docs` 为空时渲染空态提示（`rules.empty`）；系列收藏页经核实已由 `CollectionGrid` 内置 `EmptyState` 兜底，无需改动。
- 是否 Beta 必须：已解决。可否延后：已解决。

---

## Beta 风险清单专项核查

| 风险点                | 结论        | 证据                                                                                                      |
| ------------------ | --------- | ------------------------------------------------------------------------------------------------------- |
| 是否可能丢失用户数据         | ✅ 风险已显著降低 | 备份提醒（默认关闭，可配间隔）+ 一键完整数据包 + 恢复前校验不覆盖（M02/M03）；误删均有确认（M05）                                            |
| 是否没有备份/导出          | 有备份与导出    | DB 备份、完整数据包、完整收藏 CSV、卡组 JSON、心愿单/清单/卡柜/借还/联系人 CSV、缺卡 TXT/CSV；卡图支持 ZIP 批量导入                                |
| 是否没有删除确认           | 已全部覆盖     | 全部核心删除均有 `ask()/confirmAction()`；卡柜卡片移除确认已补齐（M05 已解决）                                                   |
| 是否有重复插入购买清单问题      | 已防住       | `UNIQUE(list_id, card_no, card_no_extend, language_pref, finish_pref)` + upsert；批次保存事务化；bought 写回后归零防重复 |
| 是否借出归还没有影响可用库存     | 已实现       | `available = owned − 生效借出 + 生效借入`（active/overdue 生效），归还即恢复（`checkDeckOwnership`/`getActiveLoanQty`）     |
| 是否心愿单/购买清单/库存状态不同步 | ✅ 已解决     | 写回路径齐全（acquired→收藏、bought→收藏、借入对账）；清单打开即按实时库存自愈快照，心愿单显示「已满足」徽标（M04 已解决）                                 |
| 是否没有处理空数据          | ✅ 已补齐     | `EmptyState` 覆盖收藏/心愿/借还/清单/卡组；`/rules` 空态已补（M12 已解决）                                                   |
| 是否没有错误提示           | 大部分有      | toast + 对话框广泛使用；卡组导出/复制失败已补 toast；records/logs 加载与首页仍偶发静默                                             |
| 是否没有用户反馈渠道         | 有但弱       | 设置页飞书文档链接（唯一入口）                                                                                        |
| 是否有隐私问题            | 风险低       | Local-first、无埋点、凭据不落库；注意：备份 .db 含联系人私密信息，需提示用户妥善保管（F173）                                                |
