# 回放对战棋盘 UI 布局设计

> 适用范围：`src/routes/replay/[key]/+page.svelte` → `ReplayViewer.svelte`（画布主视图）+ `ReplayBoard.svelte`（玩家半区）
> 最后更新：棋盘三行镜像布局 + 视口层顶部/底部信息带

## 1. 底层技术方案

### 1.1 固定画布 + 等比缩放（Scale to Fit）

- 画布根容器写死为 **16:9（1280 × 720）**，内部所有区域使用固定像素 / flex / grid 布局
- 缩放系数：`scale = Math.min(window.innerWidth / 1280, window.innerHeight / 720)`
- 缩放应用方式：`transform: scale(scale)` + `transform-origin: center`，由 `.canvas-stage`（flex 居中 + `overflow: hidden`）保证任何屏幕下居中且**绝不出现滚动**
- 监听 `window resize` + `orientationchange` 实时重算（`$effect` 注册/清理）

### 1.2 竖屏强制拦截

- `portrait = window.innerWidth < window.innerHeight` 时渲染全屏遮罩（`.portrait-overlay`）
- 显示旋转图标动画 + "请横屏游玩"文案（i18n：`replay.landscapeHint` / `landscapeHintSub`），隐藏画布，杜绝变形画面

### 1.3 双层级结构

| 层级       | 定位           | 是否随画布缩放            | 内容                                                                            |
| ---------- | -------------- | ------------------------- | ------------------------------------------------------------------------------- |
| **视口层** | `fixed` 于视口 | ❌ 不缩放（保持可点尺寸） | 顶部一行（信息面板 + 工具栏）、底部底栏、右栏书签、叙述浮层、终局浮层、竖屏遮罩 |
| **画布层** | `.canvas` 内   | ✅ 等比缩放               | 棋盘本体：双方半区 + 共用战场行 + 阅读器弹卡                                    |

## 2. 完整布局图（1280 × 720 画布坐标系）

```
┌────────────────────────────────────────────────────────────────────┐
│ [敌方信息面板]      [ 工具栏: ← 房间号  ⧉  ⛶ ]      [我方信息面板]   │ ← 视口层顶部一行 (fixed top:8)
├────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   敌方手牌（牌背叠放 ≤5 张 + 数量）                     y 50–170    │
│   [传奇][选定][符文区≤12][基地(最宽)][废牌堆][放逐区]    y 176–290   │ 敌方半区 y 46–296
│────────────────────────────────────────────────────────────────────│
│   比分横幅：敌方名 2:1 我方名 · 回合 n · 阶段            y 304–330   │
│   [战场1（背景=敌方战场卡图）][战场2（背景=我方战场卡图）] y 334–412  │ 共用战场行 y 304–412
│────────────────────────────────────────────────────────────────────│
│   [传奇][选定][符文区≤12][基地(最宽)][废牌堆][放逐区]    y 426–540   │ 我方半区 y 420–670
│   我方手牌（弧形排列，hover 上浮放大）                   y 546–666   │
│                                                                      │
│  [对局信息徽章] [先手选择者] [局数 tabs]                            │
│  [播放控制: ⏮ ◀ ▶ ⏭ ⏩ | 节奏 | 进度条 | 帧号 | 📜]                │ ← 视口层底栏 (fixed bottom:10)
│  [连锁 Chain: 小卡横排]                                              │
└────────────────────────────────────────────────────────────────────┘
```

## 3. 画布内区域明细

### 3.1 敌方半区（y 46–296，250px）— 镜像上半

| 行     | 高度  | 内容                                                                      |
| ------ | ----- | ------------------------------------------------------------------------- |
| 手牌行 | 120px | 牌背叠放（`handCards.slice(0, 5)`，负 margin 错位），数量标签 `手牌（n）` |
| 资源行 | 114px | `[传奇][选定][符文区][基地][废牌堆][放逐区]`                              |

### 3.2 共用战场行（y 304–412，108px）— 不镜像、双方共用

| 区               | 说明                                                                           |
| ---------------- | ------------------------------------------------------------------------------ |
| 比分横幅（26px） | 敌方名 · 比分 `opp : me` · 回合 n · 阶段徽章（行动方 accent 高亮 + `•`）       |
| 战场1 槽         | `battlefieldA`：背景 = 敌方战场卡图；槽内上/下两半分别显示**双方**在该战场的卡 |
| 战场2 槽         | `battlefieldB`：背景 = 我方战场卡图；同上                                      |
| 附加槽（可选）   | `battlefieldC` / `battlefieldToken` 有卡时自动追加第三窄槽（不丢数据）         |

### 3.3 我方半区（y 420–670，250px）— 镜像下半

| 行     | 高度  | 内容                                                                                                         |
| ------ | ----- | ------------------------------------------------------------------------------------------------------------ |
| 资源行 | 114px | 同敌方资源行                                                                                                 |
| 手牌行 | 120px | 弧形排列（每张 `rotate(±5°·i) + translateY`），hover 单张 `rotate(0) translateY(-14px) scale(1.16)` 上浮放大 |

> 镜像规则：敌方 = 手牌在上、资源在下；我方 = 资源在上、手牌在下（手牌行都朝各自玩家）。

### 3.4 资源行六元素（规则 508 参考）

| 元素         | 实现                                                                  |
| ------------ | --------------------------------------------------------------------- |
| 传奇         | 独立卡槽（竖卡 60px，hover/点击可预览；空位虚线占位）                 |
| 选定（英雄） | 独立卡槽（与传奇**分开**展示）                                        |
| 符文区       | `flex:none` **按卡数动态计算宽度**，`runeAreaCards.slice(0, 12)` 上限 |
| 基地         | `flex: 1 1 auto` **占比最宽**，accent 内发光矩形底座（88px 高）       |
| 废牌堆       | 折叠卡堆（牌背 50px + 数量角标），**点击弹 `CommonModal` 卡牌列表**   |
| 放逐区       | 同上                                                                  |

### 3.5 战场背景

- 背景容器 `.bf-bg`：`background: url('/black.jpg') center / cover`（**无本地图时显示黑色卡背图**，非蓝色）
- 有本地战场图（`selectedBattlefield` → `nameMetas` 解析）时 `CardSimpleImage`（`object-fit: cover`）覆盖其上
- 35% 暗化遮罩保证卡牌可读；**不显示战场名字标签**

## 4. 视口层元素明细

### 4.1 顶部一行（`.top-row`，fixed top:8）

```
[敌方信息面板]  [工具栏]  [我方信息面板]
```

- **信息面板**（`.top-info`，毛玻璃 36px 高，`max-width: 300px`）：玩家名 · 行动徽章 · 比分 · 能量 · 法力 · 传奇经验
  - 数据源：`playerNums()`（`board.score / floatingEnergy / floatingPower / legendXp`）
  - 窄屏（≤768px）：宽度收至 `26vw` 并隐藏资源 pills，只保留名字 + 比分
- **工具栏**（`.toolbar`，毛玻璃胶囊）：返回 · 房间号 · 右栏（书签）开关 · 全屏

### 4.2 底部底栏（`.bp-bar`，fixed bottom:10 居中，`width: min(940px, 94vw)`）

| 段                       | 内容                                                                         |
| ------------------------ | ---------------------------------------------------------------------------- |
| 对局信息（`.bp-meta`）   | 赛制/时长/会话徽章 · 先手选择者（可改）· 局数 tabs                           |
| 播放控制（`.transport`） | 跳转/步进/播放/快进/倍速 · 节奏滑杆 · 进度条 · 帧号/阶段/回合 · 对局记录开关 |
| 连锁（`.bp-chain`）      | `chainEntries` 小卡横排（nowrap 压缩），hover/点击可预览或固定进阅读器       |

- 窄屏（≤768px）：隐藏 `.bp-meta` 行，只留播放控制，减少对画布遮挡

### 4.3 右栏书签（`.side-col`，默认收起）

- 关键帧书签列表（回合/阶段/得分变化，最多 200 条），工具栏按钮展开

### 4.4 其他浮层

- **阅读器弹卡**（`.card-pop`，画布内 right/bottom）：hover 或点击棋盘卡牌时弹出 `ReplayCardReader` 大卡预览（250px 宽，随画布缩放），点击关闭解除固定
- 叙述浮层（`.narration-overlay`）：对局记录，右侧滑出
- 终局浮层（`.end-overlay`）：标记胜/负/平

## 5. 交互设计

| 交互                  | 行为                                                          |
| --------------------- | ------------------------------------------------------------- |
| 棋盘卡牌 hover        | 右侧弹卡预览（画布内）                                        |
| 棋盘卡牌 click        | 弹卡固定（阅读器常驻显示，可关闭）                            |
| 废牌堆 / 放逐区 click | `CommonModal` 居中弹窗展示该区卡牌网格（可点击固定进阅读器）  |
| 手牌 hover（我方）    | 单张上浮放大（`rotate(0) + translateY(-14px) + scale(1.16)`） |
| 卡牌 hover（通用）    | accent 色 outline 高亮                                        |
| 播放快捷键            | 空格 播放/暂停、←/→ 步进、Home/End 跳转、Esc 退出全屏同步     |

## 6. 响应式与多端适配

| 场景        | 表现                                                                                          |
| ----------- | --------------------------------------------------------------------------------------------- |
| 桌面 1080p+ | scale ≥ 1，画布放大，棋盘观感最佳                                                             |
| 桌面 720p   | scale = 1，1:1 展示                                                                           |
| 手机横屏    | scale ≈ 0.54：画布整体缩小但布局一致；视口层工具栏/底栏不缩放保持可点；底栏自动隐藏对局信息行 |
| 手机竖屏    | 全屏拦截提示"请横屏游玩"，画布隐藏                                                            |

## 7. 主题与样式规范

- 画布底色：`linear-gradient(var(--bg-secondary), var(--bg-primary))` + accent 光晕（`radial-gradient`），**深浅主题自适应**（`[data-theme='dark']` 变量自动生效）
- 面板毛玻璃：`color-mix(in srgb, var(--surface) 60–88%, transparent)` + `backdrop-filter: blur(8–14px)` + `1px solid var(--border-color)`
- 状态色：胜 `#16a34a` / 负 `#dc2626`（信息面板内不适用，能量绿 `#128378`、法力橙 `#d9730d`、比分 敌方红 `#e05252` / 我方蓝 `#4d9de0`）
- 无 `overflow: auto/scroll`（除 `CommonModal` 内部列表滚动与书签列表）

## 8. 相关文件

| 文件                                                | 职责                                                                                  |
| --------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `src/routes/replay/[key]/+page.svelte`              | 路由壳（加载对局 → 渲染 `ReplayViewer`）                                              |
| `src/lib/components/replay/ReplayViewer.svelte`     | 画布主视图：缩放/竖屏拦截、共用战场行、视口层（顶部/底部/书签）、弹卡阅读器、播放控制 |
| `src/lib/components/replay/ReplayBoard.svelte`      | 玩家半区：手牌行 + 资源行（镜像）、废牌堆/放逐区 `CommonModal`                        |
| `src/lib/components/replay/ReplayCard.svelte`       | 单卡渲染（卡图/牌背/战力/计数器/横置）                                                |
| `src/lib/components/replay/ReplayCardReader.svelte` | 阅读器（弹卡内容）                                                                    |
| `src/lib/components/ui/CommonModal.svelte`          | 通用弹窗（废牌堆/放逐区卡牌列表）                                                     |
